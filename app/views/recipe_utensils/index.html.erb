<div class="container-fluid">
    <div class="row my-5">
        <% @recipe_utensils.each do |recipe_utensil| %>
            <div class="col-md-6 py-2">
                <div class="card m-0">
                    <div class="card border-0" style="width: 100%; height: 100%">
                        <div class="card-body flex-grow-1 p-3">
                        <p><%= recipe_utensil.quantity%></p>
                        <p><%= recipe_utensil.utensil.name %></p>
                    </div> 
                </div>
            </div> 
        <% end %> 
    </div>
</div>

<!-- Button trigger modal -->
<button type="button" class="btn btn-primary" data-toggle="modal" data-target="#exampleModal">
  Add Utensil
</button>

<%= link_to "Go to Recipe", recipe_path(@recipe), class: "btn btn-warning" %>

<!-- Modal -->
<div class="modal fade" id="exampleModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="exampleModalLabel">Add Utensil</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
            <%= simple_form_for [@recipe, @recipe_utensil] do |f| %>
            <%= f.input :quantity %>
            <%= f.association :utensil, input_html: { class: 'select2' } %>
            <%= f.submit "Add Utensil", class:"btn btn-primary" %>
          <% end %>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
      </div>
    </div>
  </div>
</div>